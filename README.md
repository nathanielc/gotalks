This repository contains various talks about Go.

To install the present tool, use go get:

    go get golang.org/x/tools/cmd/present

Then to present a talk run the `present` command in the top level directory.

